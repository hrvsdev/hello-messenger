import styled from "styled-components";

export default function Footer(): JSX.Element {
  return <FooterWrapper><Main>OK</Main></FooterWrapper>;
}

const FooterWrapper = styled.div`
  height: 60px;
  flex-shrink: 0;
`;

const Main = styled.div``